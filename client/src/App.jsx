import { Button, Container, Typography } from "@mui/material"

export const App = () => {
  return (
    <Container>
      <Typography>
        MUI + Vite (JavaScript)
      </Typography>

      <Button variant="contained" color="primary">
        Click me
      </Button>
    </Container>
  )
}
